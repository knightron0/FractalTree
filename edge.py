class edge():
    def __init__(self, x, y, sz, cnt):
        self.x = x
        self.y = y
        self.sz = sz
        self.cnt = cnt
        self.update
        print("hello")
    def update():
        translate(x, y)
        line(0, 0, 0, -sz)
        # if cnt < 5:
        #     translate(0, -sz)
        #     rotate(a)
        #     e1 = edge(0, 0, sz * (0.67), cnt+1)
        #     e1.update()
        #     rotate(-a)
        #     rotate(-a)
        #     e2 = edge(0, 0, sz + (0.67), cnt+1)
        #     e2.update()
